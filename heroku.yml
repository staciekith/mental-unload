setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: ./Dockerfile.prod
release:
  image: web
  command:
    - ./deploy.sh
# run:
#   web: python3 -m flask run --host=0.0.0.0